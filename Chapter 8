Module :（函数） random,OS,pickle
         1 import random  ：
                  random.randint(1,10) #生成随机数
         2 import os ：(str要带有"")
                  os.getcwd() #获得当前目录
                  os.chdir(path)  #改变当前工作目录
                  os.listdir(path="") #查看子文件和子目录
                  os.mkdir(path) #创建文件夹，如果文件夹存在，抛出 FileExistsError异常
                  os.makedirs(path) #创建多层目录 a(b(c))
                  os.remove(path) #删除文件 path指文件名
                  os.rmdir(path) #删除目录 path指文件夹
                  os.removedirs(path) #删除多层目录 os.removedirs(r"a\b\c")
                  os.rename(old,new) #重命名文件or文件夹
                  os.system(command) # calc是Windows自带的计算器
                  walk(top） #遍历 for i in os.walk("D"): 
                  os.basename(path) os.dirname(path) #获得文件名和路径名
      
  
      
      
文件
OS
函数
